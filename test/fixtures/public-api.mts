import sdk, { api, project, router, version } from 'gl-sdk4-plugin-kit';
import { glApiMixin } from 'gl-sdk4-plugin-kit/browser';

const rootVersion: string = sdk.version;
const namedVersion: string = version;

void api.createApiClient;
void project.check;
void router.inspect;
void glApiMixin;
void rootVersion;
void namedVersion;
